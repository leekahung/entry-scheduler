/**
 * How long each free-text field may be.
 * Shared with the console, whose inputs carry the same limits so an
 * over-length value is stopped as it is typed rather than at the server.
 */
export const MAX_NAME = 80;
export const MAX_NOTE = 280;
export const MAX_ADMIN_NOTE = 500;
export const MAX_PHONE = 30;
